# AI Prompt Library

Each milestone has its own implementation prompt.

A new AI conversation should begin for every milestone.

The agent should only implement one milestone at a time.

Previous milestones should not be modified unless explicitly requested.
