/** Wiki Renderer stage. */
export type { WikiRenderer, ResolvedIdentity } from "./WikiRenderer.js";
export {
  createWikiRenderer,
  renderPages,
  buildSlugIdentities,
} from "./createWikiRenderer.js";
