/**
 * Analyzer Registry — public surface: the `createAnalyzerRegistry()` factory and the
 * `AnalyzerRegistry` contract.
 */

export { createAnalyzerRegistry } from "./createAnalyzerRegistry.js";
export type { AnalyzerRegistry } from "./createAnalyzerRegistry.js";
