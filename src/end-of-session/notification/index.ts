/**
 * Notification seam — public surface: the `NotificationPort` and its v1 no-op
 * implementation.
 */

export { createNoopNotificationPort } from "./createNoopNotificationPort.js";
export type { NotificationPort } from "./NotificationPort.js";
