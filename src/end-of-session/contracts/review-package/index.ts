/**
 * ReviewPackage — public surface of the human-readable projection contract (EOS-D4).
 * Exposes the schema, validator, and inferred immutable type.
 */

export { reviewPackageSchema, parseReviewPackage } from "./schema.js";

export type { ReviewPackage } from "./types.js";
