# CB-010 — Implement Provider Execution

> **Status:** Planned
>
> **Related Specification:** SPEC-002 — Context Builder Agent
>
> **Implementation Package:** SPEC-002
>
> **Milestone:** M2 — Knowledge Collection
>
> **Task Type:** Service

---

# Purpose

Implement deterministic execution of registered Knowledge Providers.

This task introduces the first runtime behaviour of the Collection Engine.

Providers are executed against the registry injected during CB-007. Successful providers contribute KnowledgeItems; failing providers contribute CollectionErrors. The engine assembles both into a CollectionResult.

Collection is **partial**: a single provider failure never aborts collection.

No knowledge selection, ranking, filtering, or Context Package generation is performed.

---

# Objective

Implement deterministic provider execution using the contracts established during Milestone 1 and CB-008 / CB-009.

The implementation should provide:

- provider execution
- deterministic execution order
- KnowledgeItem aggregation
- CollectionError surfacing (using the CB-008 contract)
- CollectionResult assembly (items + errors)
- documentation

The goal is to establish deterministic knowledge collection behaviour.

---

# Scope

## Included

- Provider execution
- KnowledgeItem aggregation
- CollectionError surfacing (via the CB-008 contract)
- CollectionResult assembly (items + errors)
- deterministic execution
- immutable output
- documentation

## Not Included

- retry behaviour
- recovery logic
- logging
- error policy
- Context Builder integration
- ranking
- filtering
- duplicate handling
- Context Package generation
- explainability
- business logic beyond provider execution

---

# Design Principles

Provider execution should be:

### Deterministic

Providers execute in a predictable order.

Registry order is authoritative. Provider completion order must never influence CollectionResult ordering.

---

### Partial

A single provider failure never aborts collection.

A failing provider yields a CollectionError; successful providers still contribute their KnowledgeItems.

---

### Stateless

Execution should not create persistent runtime state.

---

### Provider-Agnostic

The Collection Engine should know only the KnowledgeProvider contract.

It should not contain provider-specific logic.

---

### Immutable

Collected KnowledgeItems and CollectionErrors become immutable before being returned.

---

### Contract-Reusing

Failures are surfaced using the CollectionError contract from CB-008.

This task does not define a new error contract and does not introduce retry, recovery, logging, or policy.

---

# References

## Architecture

- ARCH-001

## Standards

- AJS-001
- AJS-002
- AJS-004

## Specifications

- SPEC-002

---

# Dependencies

## Requires

- CB-001 completed
- CB-002 completed
- CB-003 completed
- CB-004 completed
- CB-005 completed
- CB-006 completed
- CB-007 completed
- CB-008 completed
- CB-009 completed

## Enables

- CB-011 — Context Builder Integration
- CB-012 — Collection Behaviour Tests

---

# Inputs

- KnowledgeRequest
- ProviderRegistry (via the engine's construction-time injection)
- KnowledgeProvider
- KnowledgeItem
- CollectionError
- CollectionResult

---

# Expected Outputs

Provider execution should produce:

```text
CollectionResult (KnowledgeItems + CollectionErrors)
```

---

# Behaviour

The Collection Engine should:

1. receive a KnowledgeRequest
2. execute registered providers in registry order
3. collect KnowledgeItems from successful providers
4. surface a CollectionError for each failing provider (using the CB-008 contract)
5. preserve deterministic ordering (registry order authoritative; completion order irrelevant)
6. assemble and return an immutable CollectionResult containing both items and errors

No further processing is performed.

---

# Implementation Notes

Use:

- async execution
- deterministic ordering by registry index, independent of resolution timing
- immutable collections
- provider abstraction
- the CollectionError contract (CB-008)
- the CollectionResult contract (CB-009)

Avoid:

- retry
- recovery
- logging
- error policy
- ranking
- duplicate detection
- filtering
- provider-specific behaviour
- Context Package generation

Provider completion order must never influence CollectionResult ordering — map results back to registry order.

---

# Validation

Verify that:

- Providers execute successfully.
- A failing provider yields a CollectionError rather than aborting collection.
- Successful and failing providers can appear in the same CollectionResult.
- Execution order is deterministic and follows registry order.
- Returned KnowledgeItems and CollectionErrors are immutable.
- Repository builds successfully.
- Existing tests continue to pass.

---

# Acceptance Criteria

- [ ] Provider execution implemented.
- [ ] Deterministic execution order implemented (registry order authoritative).
- [ ] KnowledgeItems collected.
- [ ] CollectionErrors surfaced via the CB-008 contract.
- [ ] CollectionResult (items + errors) returned.
- [ ] Immutable output returned.
- [ ] Repository builds successfully.
- [ ] Existing tests passing.
- [ ] Documentation updated.
- [ ] Ready for CB-011.

---

# Definition of Done

This task is complete when:

- [ ] Acceptance criteria satisfied.
- [ ] Code reviewed.
- [ ] Changes committed.
- [ ] Worklog updated.
- [ ] Milestone progress updated.

---

# Coding Agent Instructions

Read:

- implementation/CLAUDE.md
- implementation/review/MILESTONE-PLANNING.md
- ARCH-001
- AJS-001
- AJS-002
- AJS-004
- SPEC-002
- implementation README
- implementation MILESTONES
- this task document

Before implementing:

1. Summarize your understanding.
2. Explain your implementation plan.
3. Identify ambiguities.

Implement **CB-010 only**.

Do not:

- define a new error contract (reuse CB-008)
- implement retry logic
- implement recovery behaviour
- implement logging or error policy
- integrate the Context Builder
- implement ranking
- implement duplicate handling
- implement Context Package generation

If implementation reveals a better execution model:

Stop.

Explain the reasoning.

Do not silently redesign the platform.

After implementation:

- run validation
- summarize changes
- list modified files
- explain implementation decisions
- suggest a Worklog entry
- update task status
- update milestone progress
- state whether every acceptance criterion has been satisfied

If you identify a reusable engineering principle, document it separately without modifying AJS documents.

---

# Worklog Summary

_Date_

(To be completed after implementation.)

---

# Lessons Learned

(To be completed after implementation.)

---

# Change Log

| Date       | Version | Description                                                                                     |
| ---------- | ------- | ----------------------------------------------------------------------------------------------- |
| 2026-07-08 | 2.0     | Milestone reorder: this task now implements **Provider Execution** (previously the Collection Error Model). Execution surfaces CollectionErrors via CB-008 and assembles a CollectionResult under the partial-collection model; no retry/recovery/logging/policy. |
| 2026-07-08 | 1.0     | Initial task created (as Collection Error Model).                                               |

---

> **Engineering Rule**
>
> Provider execution collects knowledge and surfaces failures using established contracts.
>
> It does not retry, recover, log, rank, select, assemble, or explain.
>
> Those responsibilities belong to later milestones.
