# CB-009 — Define CollectionResult Contract

> **Status:** Completed
>
> **Related Specification:** SPEC-002 — Context Builder Agent
>
> **Implementation Package:** SPEC-002
>
> **Milestone:** M2 — Knowledge Collection
>
> **Task Type:** Contract

---

# Purpose

Define the immutable contract representing the complete deterministic outcome of knowledge collection.

The CollectionResult becomes the canonical output of the Collection Engine and the input to future knowledge selection.

Because the Context Builder uses **partial collection with deterministic error reporting**, a CollectionResult represents the whole outcome and therefore contains **both**:

- collected KnowledgeItems
- collected CollectionErrors

This task defines only the contract.

No collection behaviour is implemented.

---

# Objective

Create a stable, runtime-safe contract for the output of the Collection Engine.

The implementation should provide:

- CollectionResult schema (items + errors + metadata)
- runtime validation
- immutable contract
- TypeScript types
- public exports
- documentation

The goal is to establish the platform contract for the complete outcome of collection.

---

# Scope

## Included

- CollectionResult schema
- KnowledgeItem collection
- CollectionError collection
- runtime validation
- immutable contract
- TypeScript types
- public exports
- documentation

## Not Included

- provider execution
- collection engine behaviour
- provider registry
- knowledge selection
- ranking
- Context Package generation
- business logic

---

# Design Principles

The CollectionResult should be:

### Immutable

Collection results are read-only after creation.

---

### Complete

The result represents the full deterministic outcome of collection — both items and errors.

---

### Deterministic

The same collected knowledge always produces the same CollectionResult structure.

---

### Minimal

Contain only stable platform concepts.

---

### Portable

Independent of implementation details.

---

### Extensible

Future milestones may enrich behaviour without changing this public contract.

---

# References

## Architecture

- ARCH-001

## Standards

- AJS-001
- AJS-002
- AJS-004

## Specifications

- SPEC-002

---

# Dependencies

## Requires

- CB-001 completed
- CB-002 completed
- CB-003 completed
- CB-004 completed
- CB-005 completed
- CB-006 completed
- CB-007 completed
- CB-008 completed

## Enables

- CB-010 — Provider Execution
- CB-011 — Context Builder Integration

---

# Inputs

- KnowledgeItem
- CollectionError

---

# Expected Outputs

Example project structure:

```text
src/context-builder/

collection/
    schema.ts
    types.ts
    index.ts

index.ts
```

Folder names may evolve if implementation reveals a better organization.

---

# Public Contract

## CollectionResult

Represents the canonical, complete output of deterministic knowledge collection.

The contract should contain only stable platform concepts.

Examples include:

- collected KnowledgeItems
- collected CollectionErrors
- collection metadata

The exact schema should remain independent of execution strategy. An empty error collection is a valid result; a result containing both items and errors is a valid partial outcome.

---

# Implementation Notes

Use:

- Zod
- TypeScript inference
- immutable structures
- explicit public exports
- the CollectionError schema established in CB-008

Avoid:

- execution-specific fields
- ranking information
- selection information
- Context Package information
- provider implementation details

If additional fields appear necessary, justify why they represent stable platform concepts before introducing them.

---

# Validation

Verify that:

- A CollectionResult with only items validates successfully.
- A CollectionResult with both items and errors validates successfully.
- A CollectionResult with an empty error collection validates successfully.
- Invalid CollectionResults fail validation.
- Public exports compile.
- Repository builds successfully.
- Existing tests continue to pass.

---

# Acceptance Criteria

- [x] CollectionResult schema implemented (items + errors + metadata).
- [x] CollectionError collection included.
- [x] Runtime validation implemented.
- [x] TypeScript types exported.
- [x] Public contract exported.
- [x] Immutable contract implemented.
- [x] Repository builds successfully.
- [x] Existing tests passing.
- [x] Documentation updated.
- [x] Ready for CB-010.

---

# Definition of Done

This task is complete when:

- [x] Acceptance criteria satisfied.
- [ ] Code reviewed.
- [ ] Changes committed.
- [x] Worklog updated.
- [x] Milestone progress updated.

---

# Coding Agent Instructions

Read:

- implementation/CLAUDE.md
- implementation/review/MILESTONE-PLANNING.md
- ARCH-001
- AJS-001
- AJS-002
- AJS-004
- SPEC-002
- implementation README
- implementation MILESTONES
- this task document

Before implementing:

1. Summarize your understanding.
2. Explain your implementation plan.
3. Identify ambiguities.

Implement **CB-009 only**.

Do not:

- implement provider execution
- implement collection behaviour
- implement Provider Registry integration
- implement knowledge selection
- implement ranking
- implement Context Package generation

If implementation reveals a better contract:

Stop.

Explain the reasoning.

Do not silently redesign the platform.

After implementation:

- run validation
- summarize changes
- list modified files
- explain implementation decisions
- suggest a Worklog entry
- update task status
- update milestone progress
- state whether every acceptance criterion has been satisfied

If you identify a reusable engineering principle, document it separately without modifying AJS documents.

---

# Worklog Summary

_2026-07-08_

Implemented the **CollectionResult contract** (CB-009) — the canonical, complete,
deterministic outcome of knowledge collection under the partial-collection model.
The contract aggregates three stable platform concepts: `metadata` (provenance —
the answered `KnowledgeRequest`), `items` (`KnowledgeItem[]`, CB-004) and `errors`
(`CollectionError[]`, CB-008). It is a pure **data** contract: no provider
execution, engine behaviour, ranking, selection, dedup, retry, logging, timing or
Context Package generation.

Followed the established data-contract pattern (CB-003/CB-004/CB-008): a new
`collection/result/` module with `.strict()` `schema.ts` + module-local
`deepFreeze` + `parseCollectionResult()` (validate + deep-freeze), a
`DeepReadonly` `types.ts`, and an `index.ts` barrel, re-exported through
`collection/index.ts` and the top-level `context-builder/index.ts`. The `items`,
`errors` and `metadata` shapes **compose** the existing CB-004/CB-008 schemas
rather than redefining them.

Validation: `npm test` (93 passing across 8 files, incl. 13 new CollectionResult
tests), `npm run typecheck`, `npm run build` — all green.

---

# Lessons Learned

- **Compose contracts; do not redefine them.** `items`, `errors` and `metadata`
  all reuse existing platform schemas (`knowledgeItemSchema`,
  `collectionErrorSchema`, `knowledgeRequestSchema`). Reusing the request schema
  as provenance metadata guarantees the result can never drift from the request
  it answers — the same "reuse the source contract" move CB-004 made for a
  `KnowledgeItem`'s `source`.
- **Determinism constrains metadata.** The only defensible metadata for a
  deterministic result is provenance (the answered request). Timestamps,
  durations, retry counts and token estimates were excluded because they break
  determinism and leak execution/implementation detail — `.strict()` enforces
  their exclusion at the contract boundary.
- **A contract validates shape, not policy.** Cross-element uniqueness /
  duplicate detection was deliberately *not* enforced — that is selection
  behaviour (M3), out of scope here.

---

# Change Log

| Date       | Version | Description                                                                                       |
| ---------- | ------- | ------------------------------------------------------------------------------------------------- |
| 2026-07-08 | 2.1     | Task implemented and completed. CollectionResult contract (`metadata` + `items` + `errors`) added under `collection/result/`, composing CB-004/CB-008; validation green. |
| 2026-07-08 | 2.0     | Milestone reorder: this task now defines the **CollectionResult Contract** (previously Provider Execution) and embeds both KnowledgeItems and CollectionErrors per the partial-collection model. |
| 2026-07-08 | 1.0     | Initial task created (as Provider Execution).                                                     |

---

> **Engineering Rule**
>
> CollectionResult is the complete deterministic outcome of collection — items and errors together.
>
> It represents collected knowledge, not selected knowledge.
>
> Selection, ranking, and Context Package generation belong to later milestones.
