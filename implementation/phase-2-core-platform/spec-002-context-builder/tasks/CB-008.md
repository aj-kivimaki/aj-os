# CB-008 — Define Collection Error Contract

> **Status:** Completed
>
> **Related Specification:** SPEC-002 — Context Builder Agent
>
> **Implementation Package:** SPEC-002
>
> **Milestone:** M2 — Knowledge Collection
>
> **Task Type:** Contract

---

# Purpose

Define the immutable contract that represents a single knowledge collection failure.

The Context Builder uses a **partial collection with deterministic error reporting** model. Collection never fails because a single provider fails. A provider may contribute either KnowledgeItems or a CollectionError.

The CollectionError is therefore a first-class platform contract, defined **before** the CollectionResult so the result can embed collected errors alongside collected items.

This task defines only the error contract.

No collection behaviour is implemented.

---

# Objective

Create a stable, runtime-safe contract for collection errors.

The implementation should provide:

- CollectionError schema
- runtime validation
- immutable contract
- TypeScript types
- public exports
- documentation

The goal is to establish deterministic failure semantics for the Collection Engine.

---

# Failure Model

AJ-OS Context Builder collection is **partial**, not fail-fast.

- A single provider failure never aborts collection.
- A failing provider contributes a `CollectionError` instead of KnowledgeItems.
- Errors are collected deterministically and travel alongside items in the `CollectionResult` (CB-009).

---

# Scope

## Included

- CollectionError schema
- runtime validation
- immutable contract
- TypeScript types
- public exports
- documentation

## Not Included

- retry behaviour
- recovery logic
- logging
- provider implementation changes
- provider execution
- CollectionResult (defined in CB-009)
- Context Builder integration
- ranking
- Context Package generation

---

# Design Principles

The Collection Error Model should be:

### Deterministic

The same failure should always produce the same error structure.

---

### Explicit

Errors are represented as platform contracts rather than implementation-specific exceptions.

---

### Immutable

Collection errors are immutable after creation.

---

### Provider-Agnostic

The model should not expose provider implementation details.

---

### Extensible

Future milestones may extend behaviour without changing the public contract.

---

# References

## Architecture

- ARCH-001

## Standards

- AJS-001
- AJS-002
- AJS-004

## Specifications

- SPEC-002

---

# Dependencies

## Requires

- CB-001 completed
- CB-002 completed
- CB-003 completed
- CB-004 completed
- CB-005 completed
- CB-006 completed
- CB-007 completed

## Enables

- CB-009 — CollectionResult Contract
- CB-010 — Provider Execution
- CB-011 — Context Builder Integration
- CB-012 — Collection Behaviour Tests

---

# Inputs

- KnowledgeRequest
- KnowledgeProvider (a provider `id` identifies the failing source)

---

# Expected Outputs

Example project structure:

```text
src/context-builder/

collection/
    errors/
        schema.ts
        types.ts
        index.ts

index.ts
```

Folder names may evolve if implementation reveals a better organization.

---

# Public Contract

## CollectionError

Represents a deterministic platform description of a single collection failure.

The contract should contain only stable platform concepts.

Examples include:

- error identifier
- provider identifier
- failure category
- human-readable message

The exact schema should remain independent of provider implementation.

---

# Implementation Notes

Use:

- Zod
- TypeScript inference
- immutable structures
- explicit public exports

Avoid:

- retry logic
- provider-specific exceptions
- stack traces
- implementation-specific metadata

If additional fields appear necessary, justify why they represent stable platform concepts before introducing them.

---

# Validation

Verify that:

- CollectionError validates successfully.
- Invalid CollectionErrors fail validation.
- Public exports compile.
- Repository builds successfully.
- Existing tests continue to pass.

---

# Acceptance Criteria

- [x] CollectionError schema implemented.
- [x] Runtime validation implemented.
- [x] TypeScript types exported.
- [x] Public contract exported.
- [x] Immutable contract implemented.
- [x] Repository builds successfully.
- [x] Existing tests passing.
- [x] Documentation updated.
- [x] Ready for CB-009.

---

# Definition of Done

This task is complete when:

- [x] Acceptance criteria satisfied.
- [ ] Code reviewed.
- [ ] Changes committed.
- [x] Worklog updated.
- [x] Milestone progress updated.

---

# Coding Agent Instructions

Read:

- implementation/CLAUDE.md
- implementation/review/MILESTONE-PLANNING.md
- ARCH-001
- AJS-001
- AJS-002
- AJS-004
- SPEC-002
- implementation README
- implementation MILESTONES
- this task document

Before implementing:

1. Summarize your understanding.
2. Explain your implementation plan.
3. Identify ambiguities.

Implement **CB-008 only**.

Do not:

- implement provider execution
- implement collection behaviour
- implement CollectionResult
- implement retry or recovery
- implement logging
- implement Provider Registry integration
- implement ranking
- implement Context Package generation

If implementation reveals a better error contract:

Stop.

Explain the reasoning.

Do not silently redesign the platform.

After implementation:

- run validation
- summarize changes
- list modified files
- explain implementation decisions
- suggest a Worklog entry
- update task status
- update milestone progress
- state whether every acceptance criterion has been satisfied

If you identify a reusable engineering principle, document it separately without modifying AJS documents.

---

# Worklog Summary

_2026-07-08_

Implemented the **CollectionError** contract — the first Milestone M2 data
contract — following the contract pattern established by CB-003/CB-004. Added
`src/context-builder/collection/errors/` (`schema.ts`, `types.ts`, `index.ts`):
a `.strict()` Zod schema `{ id, providerId, category, message }`, a closed
`FAILURE_CATEGORIES` enum (`invalid-request` · `provider-unavailable` ·
`provider-error`), inferred `DeepReadonly` types, and `parseCollectionError()`
(validate + deep-freeze). Wired public exports through the `collection/` and
`context-builder/` barrels, added `tests/context-builder/collection-errors.test.ts`
(9 cases), and documented the contract in the module README.

`CollectionError` is a plain **data contract, not a JS `Error`** — no stack
traces, exceptions, timestamps or runtime objects — matching the
partial-collection model where errors travel alongside items rather than aborting
collection. No collection behaviour, provider execution, `CollectionResult`,
retry, recovery or logging was implemented.

Validation: `npm test` → 80 passing across 7 files (was 71/6); `npm run typecheck`
and `npm run build` clean.

---

# Lessons Learned

- **Represent failures as contracts, not exceptions.** Modelling a failure as a
  validated, frozen POJO (rather than an `Error` subclass) keeps it deterministic,
  provider-agnostic and serialisable — the prerequisite for embedding it beside
  items in the CB-009 `CollectionResult`.
- **A closed `z.enum` category is a boundary, not just a value.** Making
  `FAILURE_CATEGORIES` a closed set stops providers leaking implementation-specific
  error codes into the platform contract — the same discipline that keeps
  `SECTION_KINDS`/`REFERENCE_TYPES` provider-agnostic.
- **Empty is not an error.** A provider that finds nothing contributes an empty
  item set; a `CollectionError` is reserved for an actual failure. Encoding this
  distinction in the docs prevents CB-010 from conflating the two.

---

# Change Log

| Date       | Version | Description                                                                                        |
| ---------- | ------- | -------------------------------------------------------------------------------------------------- |
| 2026-07-08 | 3.0     | Task implemented: CollectionError schema, validation, immutable types, `parseCollectionError()`, public exports, tests and documentation. Status → Complete. |
| 2026-07-08 | 2.0     | Milestone reorder: this task now defines the **Collection Error Contract** (previously CollectionResult), placed before CollectionResult to support the partial-collection model. |
| 2026-07-08 | 1.0     | Initial task created (as CollectionResult Contract).                                               |

---

> **Engineering Rule**
>
> Collection failures are represented by stable platform contracts.
>
> Error representation is separate from error handling.
>
> Under partial collection, errors travel alongside items rather than aborting collection.
