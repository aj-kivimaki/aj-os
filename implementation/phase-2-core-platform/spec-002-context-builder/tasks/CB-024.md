# CB-024 — Permanent Assembly Behaviour Tests

**Status:** Done

> Milestone 4 — Context Assembly. Inherits the frozen M4 Planning Package and the
> frozen architecture (AD-001–AD-010). This document plans the task only; it
> introduces no product code. CB-024 authors the permanent regression suites for
> the Assembly stage, on the CB-006 testing foundation (mirrors CB-018).

---

# 1. Purpose

Author the permanent behaviour tests that lock in the Assembly stage's guarantees —
determinism, immutability, order preservation, empty-section and minimal-field
presence, contract conformance, and the `build()` pipeline — validated **only
through the public surface** (AJS-007 §6.1 Public-Surface Validation).

---

# 2. Scope

- **Engine boundary:** `createAssemblyEngine()` returns an immutable, stateless
  handle.
- **Determinism:** repeated `assemble` / `build` on identical inputs deep-equal
  (full-package equality, since `generated_at` is an injected input).
- **Immutability:** output is deep-frozen; the input `SelectionResult` is untouched
  (immutability by divergence).
- **Order preservation:** `references` and section contents mirror the canonical
  `selectedItems` order (RC-6).
- **Structure:** the four named sections are present and empty; minimal
  `explainability`/`summary` present; referential integrity holds.
- **Contract conformance:** `parseContextPackage()` accepts every produced package;
  deliberately malformed inputs are rejected (duplicate ids/kinds, dangling
  references).
- **Pipeline:** `build(request)` runs Collection → Selection → Assembly and returns
  `ContextPackage`; thin-orchestration equality (`build == assemble(select(collect))`).
- **Scope-negative:** no rendered output; explainability entries empty; profile inert.

---

# 3. Inputs

Test fixtures — `KnowledgeItem`s and `SelectionResult`s built through the **public**
factories/contracts. No internal modules are imported.

---

# 4. Outputs

Permanent regression suites under `tests/context-builder/`. No product code.

---

# 5. Dependencies

**Requires:**
- CB-019, CB-020, CB-021, CB-022, CB-023 — the Assembly stage and its integration
- CB-006 — the contract-testing foundation

**Enables:**
- M4 Freeze Review

---

# 6. Implementation Notes

- Validate exclusively through the public API (`createAssemblyEngine`/`assemble`,
  `build`, `parseContextPackage`); import no internal mapping or policy — the
  implementation stays free to evolve (CB-018 precedent).
- Determinism asserted by identical-inputs deep-equality; immutability by divergence
  (out-of-canonical-order input, order-preserved output; frozen output).
- Prove thin orchestration by equality with a manual stage composition.
- Include scope-negative assertions so excluded capabilities cannot silently appear
  (no rendering, empty named sections, no computed explainability, profile inert).

---

# 7. Explicit Exclusions

- No product or behaviour code; no new contracts.
- No rendering, semantic-validation, explainability, or profile tests (out of M4
  scope, AD-009).
- No internal-module imports.
- No modification of any frozen contract or of the frozen Planning Package.

---

# 8. Definition of Done

- [x] Engine-boundary, `assemble` (determinism/immutability/order/structure/
      conformance), and `build()` pipeline suites authored and passing.
- [x] Scope-negative assertions in place.
- [x] Suite count grows; no internal imports.
- [x] `typecheck`, `build`, `test` green.
- [x] Ready for M4 Freeze Review.

---

# 9. Validation

- `npm run typecheck` · `npm test` (full suite green, count increased) ·
  `npm run build`.

---

# 10. Documentation Updates

- `tests/context-builder/README.md` — refresh to the current permanent suite
  (contracts + collection + selection + **assembly** behaviour; updated count/files).
- Module `README.md` test-tree reference.
- Repository-root `README.md`, `ROADMAP.md`, `CHANGELOG.md` — synchronized at the
  M4 Freeze Review (named freeze-sync items; recurring drift guard from M1–M3).

---

# References

SPEC-002 · AJS-002 (Appendix B v1.0) · AJS-007 §6.1 · PIPELINE-ARCHITECTURE.md ·
M4 Planning Package · CB-006, CB-018 · AD-002, AD-007, AD-009 · RC-6.

---

# Change Log

| Date       | Version | Description            |
| ---------- | ------- | ---------------------- |
| 2026-07-10 | 1.0     | Initial task created.  |
| 2026-07-10 | 1.1     | CB-024 completed: permanent Assembly behaviour regression suites authored on the CB-006 foundation, validating **only through the public API** (no internal mapping, policy, title table or private helper imported). `assembly.test.ts` owns the Assembly Engine **boundary** (`createAssemblyEngine()` takes no construction dependency, returns a frozen plain handle exposing only `assemble`, stateless, deterministic in construction). `assembly-execution.test.ts` exercises `assemble()` **behaviour**: canonical Appendix B section ordering, the four always-present empty Reviewer Decision A sections, the complete total `source.type → section-kind` mapping and the merging of shared kinds, reference de-duplication and first-appearance ordering (RC-6), referential integrity, metadata composition (provenance reuse, injected `generatedAt`, `contextVersion` vs `contextBuilderVersion` distinction, `issue` drop), minimal present-but-not-computed explainability/summary, determinism (identical inputs → deep-equal), output deep-freeze, input `SelectionResult` immutability by divergence, public `ContextPackage` conformance, and scope-negative guarantees (no rendering, no computed explainability, no phantom sections/errors). The `build(request)` thin-orchestration pipeline equality is owned by `context-builder-pipeline.test.ts` (CB-023). Suite 161 → 205 across 15 files; no internal imports. `typecheck`, `build`, `test` (205) green. No product code, no new contract, no frozen contract or Planning Package changed. **Milestone M4 implementation complete** — ready for the M4 Freeze Review. Status → Done. |
