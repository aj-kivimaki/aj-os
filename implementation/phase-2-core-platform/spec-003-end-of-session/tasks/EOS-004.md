# EOS-004 — ReviewPackage (Projection) & SessionReport Contracts

> **Status:** Planned
>
> **Related Specification:** SPEC-003
>
> **Implementation Package:** SPEC-003 — End-of-Session Workflow
>
> **Milestone:** M1 — Foundation & Contracts

---

# Purpose

Define the human-readable projection and the execution log.

> **Why does this task exist?** SPEC-003 emits a review package and records logs.
> Per EOS-D4 the review package is a *projection* of the canonical
> `CandidateKnowledge[]`, not a primary artifact; and per SPEC-003 §16 the workflow
> records an execution log. This task fixes both shapes so M4/M5 can produce them.

---

# Objective

Define two immutable contracts:

- **`ReviewPackage`** — a human-readable projection derived from
  `CandidateKnowledge[]` + `Session`: `{ sessionId, generatedAt, summary,
  candidateIds[], markdown }`. Explicitly typed and documented as a *projection*
  (non-canonical, regenerable).
- **`SessionReport`** — the structured execution log (SPEC-003 §16):
  `{ sessionId, trigger, startedAt, endedAt, durationMs, analyzersRun[],
  filesAnalyzed, candidatesProduced (count + ids), errors[], result:
  completed|partial|failed, logEntry }`.

---

# Scope

## Included

- Zod schemas + inferred types + deep-freeze for both contracts
- Public exports (`SessionReport` from `contracts/`; `ReviewPackage` from
  `contracts/`, documented as a projection)
- Documentation cross-referencing EOS-D4 (canonical vs. projection)

## Not Included

- The projector that *renders* the `ReviewPackage` markdown (M5)
- The logic that *computes* the report (M4/M5)
- Persistence (M4 Review Store)

---

# References

## Architecture
- ARCH-002 (structured-canonical / rendered-projection), docs/architecture/CONTRACTS.md

## Standards
- AJS-005 §4 (observability), AJS-006 §2 (derived artifacts), AJS-007

## Specifications
- SPEC-003 §8 (Outputs), §16 (Logging)

## Related Tasks
- Requires EOS-002, EOS-003; enables M4 (report persistence), M5 (projection)

---

# Dependencies

## Requires
- EOS-002 (Session), EOS-003 (CandidateKnowledge)

## Enables
- M4 (persist `SessionReport`), M5 (render `ReviewPackage`)

---

# Inputs

- SPEC-003 §8/§16; EOS-D4 decision

---

# Expected Outputs

- `ReviewPackage` and `SessionReport` contracts exported and validated;
  `ReviewPackage` clearly typed/documented as a projection of the canonical
  candidates.

---

# Implementation Notes

- `ReviewPackage.markdown` is opaque rendered text; the package holds
  `candidateIds` (references), not embedded canonical candidate records — the
  candidates live in the store as the source of truth (EOS-D4).
- `result` is a closed enum; `errors` reuse a shared error shape (align with the
  `AnalyzerError` defined in EOS-005 where practical).
- Keep both contracts free of a clock — `generatedAt`/timestamps are injected at
  composition (CB `now` precedent), not read inside the contract.
- Do not compute anything here; these are shape-only contracts.

---

# Validation

- Valid packages/reports parse and deep-freeze; invalid `result` values and
  missing required fields are rejected; a `ReviewPackage` with `candidateIds` not
  matching provided candidates is out of scope for the contract (enforced at
  projection time, M5) — the contract validates shape only.

---

# Acceptance Criteria

- [ ] `ReviewPackage` schema/type implemented, documented as a projection.
- [ ] `SessionReport` schema/type implemented (SPEC-003 §16 fields).
- [ ] Public exports created.
- [ ] Contract tests (valid/invalid/immutable) pass.
- [ ] Ready for M4/M5.

---

# Definition of Done

- [ ] Acceptance criteria satisfied.
- [ ] Code reviewed.
- [ ] Changes committed.
- [ ] Worklog updated.

---

# Worklog Summary

_Pending._

---

# Lessons Learned

_Captured on completion._

---

# Change Log

| Date | Version | Description |
| ---- | ------- | ----------- |
| 2026-07-15 | 1.0 | Task created |

---

> **Engineering Rule**
>
> The projection references the canonical data; it never becomes the source of
> truth.
