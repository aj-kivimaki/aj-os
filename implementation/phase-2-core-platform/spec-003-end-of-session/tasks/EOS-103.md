# EOS-103 — M2 Integration & Behaviour Tests

> **Status:** Complete
>
> **Related Specification:** SPEC-003
>
> **Implementation Package:** SPEC-003 — End-of-Session Workflow
>
> **Milestone:** M2 — Session Change Collection

---

# Purpose

Prove the M2 Collection stage works **end to end**: the `GitChangeAnalyzer`,
registered in the `AnalyzerRegistry` and driven by the `collectChanges` execution
stage, produces a correct, deterministic `ChangeSet` from a real repository —
and remains partial in the face of a failing analyzer.

> **Why does this task exist?** EOS-101 (execution) and EOS-102 (analyzer) are
> each unit-tested in isolation with fixtures/stubs. This task supplies the two
> pieces that only make sense together: the **real git-backed `GitPort` adapter**
> (the thing that makes a run real) and the **integration/behaviour tests** that
> validate the whole Collection stage against a real fixture repository, closing
> M2's Definition of Done.

---

# Objective

- Implement the **real git-backed `GitPort` adapter** (`createGitPort`) that
  satisfies the EOS-102 `GitPort` interface by invoking **read-only** git
  commands over a resolved range. It never stages, commits, or mutates the repo.
- Wire `GitChangeAnalyzer` → `AnalyzerRegistry` → `collectChanges` and validate
  the assembled `ChangeSet` against **temp fixture repositories**.
- Validate **determinism** (same repo state ⇒ deep-equal `ChangeSet`) and
  **partial collection** (a failing analyzer alongside the git analyzer yields
  changes **and** an `AnalyzerError`, never an abort).

---

# Scope

## Included

- `createGitPort(...)` — the real read-only git adapter behind the EOS-102
  `GitPort` interface (e.g. `git diff --name-status <range>` and the working-
  tree/staged status, as needed to populate `GitFileChange`s).
- Integration tests that build a temp git repo fixture, register the git analyzer
  (via the real adapter), run `collectChanges`, and assert the resulting
  `ChangeSet`.
- A partial-collection behaviour test: a registry containing the git analyzer
  **and** a throwing fixture analyzer → `ChangeSet` carries the git changes
  **and** one `AnalyzerError`.
- A determinism test: the same fixture repo state produces a deep-equal
  `ChangeSet` across repeated runs.

## Not Included

- New contracts or new pipeline stages (M2 introduces none beyond EOS-101/102).
- Extraction, candidate generation, persistence, projection, CLI (M3–M5).
- Any git **write** (commit/stage/branch) — permanently out of the capture
  pipeline (ADR-002, AJS-005 §7).
- Non-git analyzers (deferred).

## Fixture-repo policy

- Real git is used **only** in these integration/behaviour tests, against
  disposable temp repositories — never in the EOS-101/EOS-102 unit tests, which
  stay on fixture analyzers and the stub `GitPort` (MILESTONES: "no real git in
  unit tests"). Temp repos are created and cleaned up per test; tests must be
  hermetic (no dependence on the host repository state).

---

# References

## Architecture

- ARCH-001; ADR-002 (read-only git; no commits in the engine);
  PIPELINE-ARCHITECTURE.md (Collection stage; Integration Check)

## Standards

- AJS-005 (composition, determinism), AJS-007 (validation before freeze)

## Specifications

- SPEC-003 §2, §9 (Workflow), §15 (recoverable/fatal), §16 (report alignment)

## Related Tasks

- Requires EOS-101 (execution) + EOS-102 (analyzer + `GitPort` interface).
- Completes M2; enables M3 (extraction consumes the `ChangeSet`).
- Mirrors SPEC-002 CB-011 (integration) + CB-012 (behaviour tests).

---

# Dependencies

## Requires

- EOS-101 (`collectChanges`), EOS-102 (`GitChangeAnalyzer` + `GitPort`).

## Enables

- M3 (Knowledge Extraction consumes the `ChangeSet`).

---

# Inputs

- The EOS-102 `GitPort` interface (the adapter's contract to satisfy).
- SPEC-002 integration/behaviour-test precedent:
  [collection.test.ts](../../../../tests/context-builder/collection.test.ts),
  [collection-execution.test.ts](../../../../tests/context-builder/collection-execution.test.ts).

---

# Implementation Notes

- **Read-only git only.** The adapter runs porcelain/plumbing **read** commands
  (`diff --name-status`, `status --porcelain`, `rev-parse`, as needed). It must
  not run any command that mutates the repository or its index. Invoke git via a
  child-process call with an explicit `cwd`; treat non-zero exits as adapter
  failures surfaced to the caller (the execution stage turns an analyzer-level
  failure into an `AnalyzerError`).
- **Range handling.** The adapter reads `session.gitState.range`. The default
  session range semantics (uncommitted+staged vs. `--since <ref>`) are finalized
  where the range string is constructed (M5/CLI); EOS-103 validates the adapter
  correctly reports changes for a **given** range against a fixture repo.
- **Hermetic fixtures.** Build each temp repo with a deterministic sequence of
  git operations inside the test, assert on the resulting `ChangeSet`, and remove
  the temp dir afterward. No test may read or write the host repository.
- **Determinism.** Assert deep equality of the `ChangeSet` across two runs over
  the same fixture state; path-sorted analyzer output (EOS-102) plus registry-
  order aggregation (EOS-101) make this hold.
- **Integration Check (PIPELINE-ARCHITECTURE).** Before proposing the M2 freeze,
  confirm: the new stage has one responsibility; it communicates only through
  immutable contracts; earlier stages are unmodified; the public `run` entry
  point is unchanged (still not introduced — that is M5); no git/wiki side effect
  entered the pipeline.

---

# Validation

- End-to-end: register `GitChangeAnalyzer` (real adapter) → `collectChanges` over
  a temp fixture repo returns a `ChangeSet` whose `changes` match the fixture's
  git operations (correct `changeType`, ids, order) with no `errors`.
- Partial collection: adding a throwing fixture analyzer yields a `ChangeSet`
  with the git changes **and** exactly one `AnalyzerError`; collection is not
  aborted.
- Determinism: the same fixture state produces a deep-equal `ChangeSet` across
  runs.
- The adapter performs no repository mutation (assert via fixture state / no
  commits or index changes introduced).
- Full platform suite `npm run typecheck`, `npm run build`, `npm test` green.

---

# Acceptance Criteria

- [x] Real read-only `createGitPort` adapter implemented behind the EOS-102
      `GitPort` interface; no git writes.
- [x] `GitChangeAnalyzer` wired through the registry + `collectChanges`
      integration-tested against a temp fixture repo.
- [x] Partial-collection behaviour test passes (git changes + `AnalyzerError`).
- [x] Determinism test passes (same state ⇒ deep-equal `ChangeSet`).
- [x] Real git confined to integration/behaviour tests; unit tests stay stubbed.
- [x] M2 Integration Check satisfied; suite green.

---

# Definition of Done

- [x] Acceptance criteria satisfied.
- [x] Code reviewed.
- [x] Changes committed.
- [x] Task change log + milestone task progress updated.
- [x] M2 ready for Freeze Review.

---

# Worklog Summary

Closed M2 by proving the Collection stage end-to-end against real git and adding
the last missing piece — the real adapter behind the EOS-102 `GitPort`.

- **`createGitPort(repositoryPath)`** (`.../git/createGitPort.ts`) — the real,
  git-backed adapter, deliberately **minimal**: it only *invokes git*, *parses
  git's output*, and *produces `GitFileChange`s*. `changes(range)` runs
  `git diff --name-status -M <range>` (read-only; `-M` lets git report the
  renames the contract already models) in the injected repository directory and
  parses the tab-separated name-status lines — taking the leading status letter
  (`R100` → `R`) and, for renames/copies, the old/new path pair. No domain
  translation, filtering, retries, orchestration, or policy; repository directory
  is injected (not discovered); range is passed verbatim. Frozen handle (module
  convention). A non-zero git exit rejects → EOS-101 turns it into one recoverable
  `AnalyzerError` (partial collection).
- **Integration/behaviour tests** (`tests/end-of-session/git-collection-integration.test.ts`)
  drive the full stack — `createGitPort` → `GitChangeAnalyzer` →
  `AnalyzerRegistry` → `collectChanges` → `ChangeSet` — over **disposable temp
  fixture repos** (built with real git, torn down per test). They verify: correct
  git translation (add/modify/delete/rename → `changeType`, ids, kind, rename
  `metadata.oldPath` + summary), path-sorted **deterministic collection**
  (deep-equal across repeated runs), **registry wiring** (`get("git")` +
  execution), **end-to-end ChangeSet production**, and **partial collection**
  (git changes + one `AnalyzerError` from a failing co-analyzer; and a bad range
  surfacing as a recoverable git `AnalyzerError`, not a throw).

Exported `createGitPort` from the module barrel; recorded it in the EOS-007
drift-guard manifest (public ops now 14). Real git is confined to this
integration suite — the EOS-101/EOS-102 unit tests remain on fixture analyzers
and the stub `GitPort`.

**Code review** (high effort, 8 angles): no correctness defects. One low-severity
robustness observation — `execFile`'s default 1 MB `maxBuffer` means an
extremely large diff would fail the git analysis (degrading gracefully to a
recoverable `AnalyzerError`). Left unchanged by design: raising the cap is beyond
the frozen "minimal adapter" scope; recorded here and raised for the M2 freeze as
a candidate hardening task.

**Validation.** `npm run typecheck` + `npm run build` clean; 7 integration tests;
full platform suite green: **476 tests / 45 files**. M2 Integration Check
(PIPELINE-ARCHITECTURE): the Collection stage has one responsibility; communicates
only through immutable contracts; earlier stages unmodified; the public `run`
entry point is still not introduced (M5); **no git write or wiki side effect**
entered the pipeline.

---

# Lessons Learned

A minimal adapter is easy to keep honest when the split is stated as three verbs —
*invoke, parse, produce* — and everything else (translation, discovery, range
construction, retries) is named as belonging elsewhere; the temptation to "just
filter this here" disappears when the responsibility boundary is that explicit.
Disposable fixture repos make the end-to-end proof both real and hermetic: build
with real git in the test, assert on the `ChangeSet`, delete — no dependence on
the host repository. The `noUncheckedIndexedAccess` strictness turned sloppy
name-status indexing into explicit field guards, which is exactly the defensive
parsing a real adapter wants. Because the `Analyzer` port has no error channel, a
git failure has one honest signal — reject — and EOS-101's partial-collection
catch is what makes even a bad range safe end-to-end.

---

# Change Log

| Date | Version | Description |
| ---- | ------- | ----------- |
| 2026-07-16 | 1.1 | **Task completed** — minimal read-only `createGitPort` adapter (`git diff --name-status -M`, output parsing → `GitFileChange`s) + end-to-end integration/determinism/partial-collection tests over disposable fixture repos; drift-guard manifest updated; typecheck/build/full suite (476) green; review clean (one deferred robustness note). **M2 ready for Freeze Review.** |
| 2026-07-15 | 1.0 | Task created (M2 planning). Defines the real read-only `GitPort` adapter plus the end-to-end integration, determinism, and partial-collection behaviour tests that close M2. |

---

> **Engineering Rule**
>
> Real git lives only in disposable fixture-repo integration tests, and it is
> read-only. The capture pipeline observes changes; it never commits them.
