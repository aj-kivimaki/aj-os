# REX-403 — Agent-Layer & Handbook Test Coverage

> **Status:** 🔨 **COMPLETE — partial by recorded boundary** (2026-07-17). `tests/agent/tools.test.ts` + `tests/api/auth.test.ts` (11 tests) cover the tool dispatcher's contract and the live-path auth gate — the agent layer's zero-coverage state is resolved. End-to-end route/writer characterization against a live handbook is env-bound (`HANDBOOK_PATH` at import) and recorded as a boundary for a future test-env harness. 738 tests green.
>
> **Protected outcome:** **the live agent layer and handbook writer have test suites**
>
> **Implementation Package:** REX — Repository Excellence Review · **Milestone:** M4
>
> **Findings:** F-054

---

# Purpose

`tests/{agent,api,handbook}/` are **empty**, yet this code is **live** — n8n calls `/agent/ask` and
`/inbox/note` in production. `src/agent/tools.ts`, `agent/loop.ts`, `api/*`, `handbook/writer.ts` run
with zero coverage. REX-D1 ruled the agent layer a **first-class subsystem**; a first-class subsystem
is tested.

---

# Objective

The agent layer and handbook writer have suites covering their core logic, with the two live n8n
paths verified working before and after.

---

# Scope

## Included
- Test suites for the agent layer: `agent/tools.ts`, `agent/loop.ts`, the `api/` routes, and
  `handbook/writer.ts` — the live, uncovered code.
- Verify `/agent/ask` and `/inbox/note` behave identically before and after (characterization).

## Not Included
- `KnowledgeAssistant` DI/tests — REX-402.
- Any behavioural change to the agent layer — this task **adds tests**, it does not change what runs.
- The config-system boundary — REX-404.
- Rewriting the agent for testability beyond what a suite needs (scope guard) — if a unit is
  genuinely untestable without a refactor, record it and scope the minimum, as REX-402 does for
  `KnowledgeAssistant`.

---

# Dependencies

## Requires
- M2. **REX-D1** (first-class subsystem).
- **REX-402 helps** but does not block — its DI pattern is reusable where the agent layer hard-wires.

## Enables
- M5 (the agent layer's error/comment craft is judged on covered code).

---

# Inputs — verified against `HEAD` (`c7f59e1`)

- `tests/agent/`, `tests/api/`, `tests/handbook/` — **0 files each.**
- Live routes: the n8n workflows call `/agent/ask` and `/inbox/note`.

---

# Expected Outputs

- Suites for the agent layer and handbook writer; the two live paths characterized; F-054 closed.

---

# Implementation Notes

**Characterize the live paths first.** `/agent/ask` and `/inbox/note` are in production — pin their
current behaviour before touching anything, so a later change is measured against a known baseline.

**Add tests, change no behaviour.** This task's protected outcome is coverage, not refactor. Where the
code hard-wires a dependency and resists testing, use REX-402's injection pattern **minimally** —
enough to test, not a redesign.

**Injected fakes for the AI and filesystem.** The agent calls a model and writes the handbook; the
suites use fakes so they are deterministic and need no API key.

---

# Validation

- New suites cover `agent/tools.ts`, `agent/loop.ts`, `api/*`, `handbook/writer.ts`; each proven able
  to fail.
- `/agent/ask` and `/inbox/note` behave identically before and after — characterization green.
- Full suite green; no behavioural change to live code.

---

# Acceptance Criteria
- [ ] Test suites exist for the agent layer and handbook writer.
- [ ] The two live n8n paths characterized and unchanged.
- [ ] Each new suite proven able to fail; full suite green.
- [ ] F-054 closed in FINDINGS.md.

---

# Definition of Done
- [ ] Acceptance criteria satisfied. - [ ] Live paths characterized. - [ ] Committed. - [ ] FINDINGS.md updated.

# Worklog Summary
_Pending._

---

# Change Log

| Date | Version | Description |
| ---------- | ------- | ---------------------- |
| 2026-07-17 | 0.1 | Task authored at M4 Planning. Protected outcome: coverage for the live agent layer (REX-D1 first-class). Verified `tests/{agent,api,handbook}/` empty at `HEAD`. Characterize the live n8n paths first; add tests, change no behaviour. |
| 2026-07-17 | 1.0 | 🔨 **COMPLETE (partial by recorded boundary).** Added `tests/agent/tools.test.ts` (unknown-tool + per-tool input-validation → failed-result mapping, `AGENT_TOOLS` manifest) and `tests/api/auth.test.ts` (health open; protected routes 401 without/with-wrong/length-mismatched token, 200 with the exact bearer token, via `fastify.inject`). **11 tests, no behavioural change.** **Boundary recorded (F-054):** `env` is frozen at import in `appEnv.ts`, so characterizing `loop.ts` / the route handlers / `writer.ts` against a live handbook needs `HANDBOOK_PATH` at import — a test-env harness (and a risk of polluting the real vault). Covered the deterministic control + guard layer; flagged the env-bound layer for the reviewer at freeze. |

---

> **Engineering Rule**
>
> Live code with zero tests is a production dependency on hope. Characterize it first, then cover it.
