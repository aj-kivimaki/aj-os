# REX-203 — Resolve the 40 Hidden Errors

> **Status:** Planned
>
> **Protected outcome:** ⚠️ **executable behaviour boundary** — the only M2 task changing executable source for **non-mechanical** reasons
>
> **Implementation Package:** REX — Repository Excellence Review · **Milestone:** M2

---

# Purpose

**Why does this task exist?**

**This is the highest-value and highest-risk task in the entire package.**

REX-202 makes 40 type errors visible. They are **real strictness violations** under the repository's
own `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` — guarantees the project chose,
enforced on exactly half its code.

**The precedent is unambiguous: this gap hid three real defects during SPEC-003.** Not style issues —
a broken stub, an `exactOptionalPropertyTypes` violation, and an import of a type that does not
exist. Each passed every check the repository had.

**And two of the 40 are `TS2352` — `readonly`→mutable casts the compiler flags as *"may be a
mistake"* — in a repository whose immutability is contract-level**, where `deepFreeze` and
`DeepReadonly` are load-bearing and EOS-005 ratified their duplication rather than weaken them. **If
a real defect hides among the 40, that is where it is.**

---

# Objective

Resolve all 40 errors. **Triage each as *defect* or *type-narrowing* before fixing it** — and record
every defect as a finding rather than silencing it.

---

# Scope

## Included

- All **40** errors, **all in `tests/`, zero in `src/`** (verified). By code: 24× TS2532, 7× TS2741,
  3× TS2345, 3× TS18048, **2× TS2352**, 1× TS2379.
- **Per-error triage**, recorded.
- Any **real test defect** found: recorded as a finding, fixed **failing-test-first**.

## Not Included

- **Fixing `src/`** — there are no `src/` errors here. If one appears, the measurement was wrong:
  **halt and report**.
- Refactoring tests that merely *look* improvable. **The scope guard defers it**; only what a
  diagnostic names is in scope.
- Rewriting the duplicated test helpers (F-064) — **M5**.
- Suppressing anything with `@ts-expect-error` or `!` **to make the count go down**.

---

# Dependencies

## Requires
- **REX-202** — the errors must be visible first. **Same PR** (REX-202 alone leaves CI red).

## Enables
- The rest of M2 — no later gate can be trusted while a known-failing check is being ignored.

---

# Inputs

- The 40 errors from `tsconfig.test.json`.
- **SPEC-003's three defects** — the precedent for what to expect.
- The frozen contracts the tests validate. **A test that violates a contract's type may be right
  about the contract and wrong about the type — or the reverse.** Only reading both settles it.

---

# Expected Outputs

- `tsc -p tsconfig.test.json` → **0 errors**; a recorded triage per error; any defect found recorded
  as a finding; F-027 closed.

---

# Implementation Notes

**Triage before fixing. Every error, individually.** The temptation is a sweep of `!` and
`?.` that turns 40 into 0 in an afternoon. **That would silence precisely the signal this task
exists to surface**, and it would do it invisibly.

For each error ask: **what is this error telling me about the code under test?**

- **Type-narrowing** — the test indexes an array the contract guarantees is populated; the compiler
  cannot know. Add the guard or the assertion. **Low risk.**
- **Real defect** — the test asserts something that cannot hold, stubs a shape that no longer
  matches, or casts past a contract. **Record as a finding. Fix failing-test-first**: make the test
  fail for the right reason before making it pass.

**Start with the two `TS2352` casts** (`collection-result.test.ts:138`,
`selection-result.test.ts:174`). A `readonly`→mutable cast in a suite that exists to prove deep
immutability is **either a test that cannot fail, or a real hole in the guarantee**. SPEC-003's most
common review finding was *tests that cannot fail*. **Ask of each: what would make this fail?**

**`git-collection-integration.test.ts` carries 7 and `git-change-analyzer.test.ts` 5** — the two
suites over real git. `TS2532`/`TS18048` there mean the test assumes git returned something. **That
assumption is the test's actual subject**, so a bare `!` erases the thing being tested.

**Never weaken to go green.** No test removed, skipped, or `todo`'d; no assertion loosened; no
`expect` deleted. **[REX-D10](../decisions/REX-D10.md) makes this a milestone-level falsifier**, and
this task is where it would be violated.

**If a fix requires changing `src/`, stop.** No `src/` errors exist. A fix that reaches into `src/`
means a real defect in production code — **halt and report**; that is an FPCP conversation, not a
quiet edit.

---

# Validation

- `tsc -p tsconfig.test.json` → **0 errors**.
- **All existing tests pass** — count does not fall.
- **No `.skip` / `.only` / `todo` introduced**; `expect` count does not fall.
- Every fix traceable to its diagnostic and its triage verdict.
- **Any defect found is proven** — the test failed for the right reason first.
- `git diff --name-only -- src/` → **empty**.

---

# Acceptance Criteria

- [ ] All 40 resolved; `tsc -p tsconfig.test.json` → **0**.
- [ ] **Every error triaged** *defect vs. type-narrowing*, recorded — no unexplained fix.
- [ ] **Both `TS2352` casts** given a written verdict: is the guarantee real, or was the test unable to fail?
- [ ] Every **real defect** recorded as a finding and fixed **failing-test-first**.
- [ ] **No test removed, skipped, weakened; no `expect` deleted.**
- [ ] **No `src/` file modified** — if one is needed, **halt and report**.
- [ ] All existing tests pass.
- [ ] F-027 closed.

---

# Definition of Done

- [ ] Acceptance criteria satisfied. - [ ] Reviewed. - [ ] Committed **with REX-202**. - [ ] `FINDINGS.md` updated.

---

# Worklog Summary

_Pending._

# Lessons Learned

_Pending._

---

# Change Log

| Date | Version | Description |
| ---------- | ------- | ------------ |
| 2026-07-17 | 1.0 | Task created (M2 planning). Count corrected 46 → **40** at the Planning Re-read; all in `tests/`, none in `src/`. |

---

> **Engineering Rule**
>
> Forty errors can be silenced in an afternoon. That is the failure mode, not the goal.
>
> Ask of every one: **what is this telling me about the code under test?**
