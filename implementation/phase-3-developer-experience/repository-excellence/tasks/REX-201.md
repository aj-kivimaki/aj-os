# REX-201 — Enforcement Exists: CI on Every Push and PR

> **Status:** Planned
>
> **Protected outcome:** **governance — enforcement**
>
> **Implementation Package:** REX — Repository Excellence Review · **Milestone:** M2

---

# Purpose

**Why does this task exist?**

**`.github/` does not exist and never has** — `git log --all -- .github` returns nothing across the
repository's entire history. Eleven pull requests merged with **zero automated gates**.

Meanwhile the repository states the policy **twice**: `CONTRIBUTING.md:72` and
`docs/guides/development.md:51` both require `npm run typecheck`, `npm run build`, and `npm test`
before every pull request. **A policy stated twice and enforced zero times.**

Every other M2 task builds a gate. **This task is the thing that runs them.** Without it, M2 produces
configuration nobody executes — which is the failure mode that produced this inventory.

---

# Objective

CI runs typecheck, build, and test on every push and pull request, and is **demonstrated failing**
before it is trusted.

---

# Scope

## Included

- `.github/workflows/ci.yml` — typecheck + build + test, on `push` and `pull_request`.
- An `npm run ci` script composing the same gates locally, so the CI definition and the local command
  cannot drift.

## Not Included

- **Lint and format gates** — REX-204/205 create them; this task adds them to CI **only if they exist
  by then**, otherwise their own tasks wire them in. *(Sequencing, not scope.)*
- **Coverage** — REX-208.
- PR template, dependabot, CODEOWNERS — **REX-207** (governance *process*, a different outcome).
- `SECURITY.md`, `CODE_OF_CONDUCT.md` — **REX-206** (documentation). *These live near `.github/` and
  were allocated with it by path in the frozen plan; the re-read separated them by outcome.*
- Branch protection / required checks — **a repository setting, not a file.** Recorded as a
  recommendation for the reviewer; REX cannot set it.

---

# Dependencies

## Requires
- Nothing. **M2's first task** — every other gate needs somewhere to run.

## Enables
- REX-202, 203, 204, 205, 208 — each adds a gate to a pipeline that exists.

---

# Inputs

- `package.json` scripts: `typecheck`, `build`, `test` — the three the policy already names.
- Node version: **unpinned today** (F-033 — REX-206's). CI must pick one explicitly; use the local
  major (22) and let REX-206 make it authoritative via `engines`/`.nvmrc`.

---

# Expected Outputs

- `.github/workflows/ci.yml`; `npm run ci`; F-025 closed.

---

# Implementation Notes

**Keep it boring.** `actions/checkout` + `actions/setup-node` + `npm ci` + the three gates. No
matrix, no caching cleverness, no reusable workflows. The finding is *"nothing runs"*, not *"what runs
is unsophisticated"*.

**The pipeline must be red before it is trusted.** Push a deliberate type error, confirm CI fails,
revert. **A gate never seen red is not known to work** — SPEC-003's standard, and the reason its
canonical-unchanged proof was tampered with before being believed.

**15 test suites touch `node:fs` / `node:child_process`** and construct real git repositories.
`session-fixtures.ts` runs `git init` and commits. **CI has no git identity configured by default**,
so those suites may fail in CI while passing locally. **Verify by running CI on the branch before
requiring the check** — this is the most likely source of a red pipeline that is CI's fault rather
than the code's.

**`npm ci` requires `package-lock.json`.** Confirm it is committed; if not, that is a finding, not a
workaround.

---

# Validation

- CI green on this branch.
- **Each of the three gates demonstrated failing** on a deliberate violation, then passing.
- `npm run ci` locally runs the same gates as the workflow — verified by reading both.
- **No source or test file modified by this task.**

---

# Acceptance Criteria

- [ ] `.github/workflows/ci.yml` runs typecheck + build + test on push and PR.
- [ ] CI is **green on this branch**, having first been **seen red** for each gate.
- [ ] `npm run ci` composes the same gates.
- [ ] Node version explicitly pinned in the workflow.
- [ ] Filesystem/git test suites verified to pass **in CI**, not only locally.
- [ ] Branch protection recorded as a **recommendation** — REX cannot set repository settings.
- [ ] `git diff --name-only -- src/ tests/` → **empty** for this task.
- [ ] F-025 closed.

---

# Definition of Done

- [ ] Acceptance criteria satisfied. - [ ] Reviewed. - [ ] Committed. - [ ] `FINDINGS.md` updated.

---

# Worklog Summary

_Pending._

# Lessons Learned

_Pending._

---

# Change Log

| Date | Version | Description |
| ---------- | ------- | ------------ |
| 2026-07-17 | 1.0 | Task created (M2 planning, allocated by protected outcome). |

---

> **Engineering Rule**
>
> A policy stated twice and enforced zero times is not a policy. It is a preference.
