Retrospectives accumulate here — one per milestone (AJS-007 §4.7 stage 7, §8.1).

They are never overwritten: SPEC-FREEZE-REVIEW.md's Freeze Decision requires
"Retrospective completed (accumulated, not overwritten)".

This file exists so the directory is tracked before the first retrospective lands.
Without it the package README would document a directory that does not exist in the
repository — which is finding F-018/F-020 against SPEC-003, and not a mistake worth
repeating while reviewing it.
