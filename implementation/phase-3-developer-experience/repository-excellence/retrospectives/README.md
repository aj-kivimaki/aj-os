Retrospectives accumulate here — one per milestone (AJS-007 §4.7 stage 7, §8.1).

They are never overwritten: SPEC-FREEZE-REVIEW.md's Freeze Decision requires
"Retrospective completed (accumulated, not overwritten)".

Present:

- `RETROSPECTIVE-M1.md` — M1 Documentation Truth & SPEC-003 Lifecycle Closure (frozen 2026-07-17).

This file originally existed so the directory was tracked before the first retrospective
landed — without it the package README would have documented a directory that does not
exist, which is finding F-018/F-020 against SPEC-003 and not a mistake worth repeating
while reviewing it.
