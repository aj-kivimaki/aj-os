# AJ-OS Prompt

## Milestone

v0.5.1 – Property Translator Expansion

---

# Before You Begin

Read the following before writing any code:

- docs/
- .ai/project-context.md
- .ai/implementation-rules.md
- .ai/coding-standards.md
- .ai/engineering-philosophy.md

These documents define the project architecture.

Implement only this milestone.

Do not redesign the architecture.

---

# Your Role

You are a senior TypeScript engineer.

Your responsibility is implementation.

The existing architecture is complete.

Extend it without introducing unnecessary abstractions.

---

# Objective

The Projects module currently uses several property types that are not yet supported by the Notion Translation Layer.

Extend the existing translation system so that the Projects database can be translated successfully without modifying the Schema layer or the Projects module.

---

# Current Failure

The application currently reports the following unsupported property types:

- date
- multi_select
- url
- checkbox

The translation layer should be expanded to support these property types.

---

# Requirements

Extend the existing property translator architecture.

Implement translators for:

- date
- multi_select
- url
- checkbox

Update any existing translator registry or mapping required to register the new translators.

Update translation validation so these property types are considered supported.

Reuse the existing translation patterns.

---

# Constraints

Do NOT modify:

- Schema Engine
- DatabaseDefinition
- PropertyDefinition
- Projects Module
- Application Layer
- Database creation workflow

Do NOT implement additional Notion property types beyond those listed above.

Do NOT introduce breaking changes.

---

# Design Goals

The implementation should:

- Match the style of the existing translators
- Reuse existing abstractions
- Keep translators independent
- Preserve the clean architecture
- Avoid duplication

---

# Deliverables

Expected additions include:

```
src/notion/properties/

date.ts
multi-select.ts
url.ts
checkbox.ts
```

Update:

- translator registry
- validation
- exports (if necessary)

---

# Complexity Budget

Target approximately:

- 4–6 new files
- 100–200 lines of TypeScript

This is a small incremental improvement.

Avoid overengineering.

---

# Before Finishing

Before completing the milestone:

- Verify TypeScript compiles.
- Verify the Projects module translates successfully.
- Verify no unsupported property validation errors remain.
- Remove unused code.
- Explain any design decisions.
- Suggest improvements separately rather than implementing them.

---

# Definition of Done

The Notion Translation Layer supports:

- title
- text
- number
- select
- date
- multi_select
- url
- checkbox

The Projects database reaches the Notion API without translation validation failures.

No architectural changes are introduced.
